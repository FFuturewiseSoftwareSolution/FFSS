/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://www.ffuturewisesoftwaresolution.com', // ✅ your real domain
  generateRobotsTxt: true, // generates both sitemap.xml & robots.txt
  sitemapSize: 7000,
};

export default config;
